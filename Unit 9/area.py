class Shape:
	def __init__(self):

class Square()
	def __init__(self, size):
		self.size = size

	def are(self):
		return size * size
		
class Circle
	def __init__(s)

def calculate_area(shape):
	return shape.area()

	# if shape.is_circle == True:
	# 	radius = (shape.width / 2)
	# 	return 3.14 * radius * radius
	# elif shape.is_circle == False:
	# 	return shape.width * shape.height

	# Assume square

	circle = Shape(10,10, True)
	square = Shape(10,10, False)

	area1 = calculate_area(circle)
	rea1 = calculate_area(square)
	print(area)